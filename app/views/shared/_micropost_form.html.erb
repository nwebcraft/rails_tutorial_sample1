<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.label :content, "投稿内容" %>
    <%= f.text_area :content, placeholder: "新しい投稿を書いてください。。。" %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-large btn-primary" %>
<% end %>